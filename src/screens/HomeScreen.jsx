import React from "react";
import TodoList from "../component/ToDoList/TodoList";

const Home = (props) => {
     return (
        <React.Fragment>
            <TodoList />
        </React.Fragment>
     )
};

export default Home